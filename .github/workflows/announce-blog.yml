name: Blog announcement ownership
on: workflow_dispatch
permissions:
  contents: read
jobs:
  information:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Campaign announcements are owned by QuickPost after deployment verification."
