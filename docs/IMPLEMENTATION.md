# Reviewed site changes

Preserve the dated short-post feed and existing retro fonts. Game copy in data/planet.json is imported verbatim from Steam app 4724190 on 2026-09-07. Public game copy must not be AI rewritten. The first three mechanic sections use the original Steam videos with controls and no preload. The full official trailer and additional gallery remain dependent on suitable originals and human-authored accessibility descriptions.

The old Magic or Die demo and its spec live only in docs/retired. Deploy from a fresh artifact directory so stale pages cannot survive. QuickPost owns campaign announcements; the old automatic Discord workflow is disabled to prevent duplicates.

The missing dd0365 GIF has no matching source asset and its broken markup was removed; the post body remains. The October 2024 image now points to the existing source. Historical metadata uses dates rather than generated titles; authors can replace these later.

No form was submitted during verification. Native browser validation remains in use. Buttondown handles subscription confirmation; the local thanks route only says to check the inbox.
